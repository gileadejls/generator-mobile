import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputer:{
        padding: 10,
        width: '100%',
        textAlign: 'center',
        fontSize: 22,
        borderBottomWidth: 2,
        borderBottomColor: '#DDDDDD',
        color: 'black',
        borderRadius: 7,
        marginBottom: 10
    }
});